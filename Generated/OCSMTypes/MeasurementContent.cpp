#include <MeasurementContent.h>
#include <VehiclePose.h>

namespace OCSMTypes 
{
	vector<MeasurementContent> measure_content(vector<VehiclePose>&pos, JSONValue* json)
	{
		for (size_t i = 0; i < pos.size(); i++)
		{
			measure_content.push_back(pos[i],) = 

		}
		
	}

}
